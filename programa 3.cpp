#include<stdio.h>
#include<stdlib.h>
int main (){
	int numero;
	printf("Introduzca el numero:\n",163);
	scanf("%d", &numero);
	if (numero % 2 == 0)
	printf (" Es Par :D\n");
	else if (numero %3 == 0)
	printf ("es divisible entre 3\n");
	else 
	printf (" Es Impar D:");
}
